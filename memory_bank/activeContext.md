# Active Context
- Выполнен переход на JetBrains Mono для всех code и pre блоков (поддержка кириллицы).
- Затронутые файлы: `app/globals.css`, `app/layout.tsx`.
- Текущие решения: Импорт JetBrains_Mono в root layout и использование CSS-переменной.
