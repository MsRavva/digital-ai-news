import { MarkdownRenderer } from "@/components/markdown-renderer";

export default function TestInlineCodePage() {
  const markdownContent = `
# Тестирование вертикального выравнивания инлайн-кода

## Простой инлайн-код

Это пример текста с \`инлайн-кодом\` внутри строки.

## Инлайн-код с разным размером

Вот \`короткий\` код и вот \`очень-очень-очень-очень-длинный-код\` в одной строке.

## Инлайн-код в заголовках

### Это заголовок с \`кодом\`

## Инлайн-код в списке

- Элемент списка с \`кодом\`
- Другой элемент с \`длинным-очень-длинным-кодом\`
- Третий элемент

## Инлайн-код рядом с другими элементами

Текст с [ссылкой](/) и \`кодом\` и еще **жирным текстом**.

## Многострочный параграф с инлайн-кодом

Это первый абзац с \`инлайн-кодом\`, который содержит какие-то важные данные и используется для демонстрации выравнивания. Мы должны убедиться, что код выровнен по центру строки.

В следующем абзаце также будет \`инлайн-код\`, и он должен быть выровнен по вертикали с окружающим текстом. Это очень важно для читаемости и визуального восприятия.

## Инлайн-код в цитатах

> Это цитата с \`инлайн-кодом\`, который должен быть выровнен по вертикали с остальным текстом цитаты.

## Сравнение с обычным кодом

Вот обычный блок кода:

\`\`\`javascript
console.log('Пример кода');
\`\`\`

А вот \`инлайн-код\` в той же статье.
`;

  return (
    <div className="container mx-auto py-8 px-4 max-w-3xl">
      <h1 className="text-3xl font-bold mb-6">Тестирование вертикального выравнивания инлайн-кода</h1>
      <div className="border rounded-lg p-6 bg-white dark:bg-gray-800 shadow-sm">
        <MarkdownRenderer content={markdownContent} />
      </div>
    </div>
  );
}